# 저장한 데이터 불러오기

savePath = ('C:/Users/USER/Documents/R/2010_1%_인구사항_20230214_48383.RData')
load(savePath)
str(popData)

# 컬럼의 값별 데이터 갯수
table(...)

#xx <- pi # to ensure there is some data
#save(list = ls(all.names = T), file = "all.rda")
#rm(xx) # xx 제거


#load('all.rda') # 다시 로드, xx 불러옴
#ls()
